{
    // Workspace interpreter set to the virtual environment in the repository
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
    // Backwards compatibility for older Python extension versions
    "python.pythonPath": "${workspaceFolder}/.venv/bin/python"
}